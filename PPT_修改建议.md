# PPT 修改意见与结构调整建议

> 基于 `MARL_PPT_返_20260616_修改.pptx` 中的批注整理

---

## 一、具体页面修改意见

### Slide 1：标题页
**批注**：汇报人那里，先写做 pre 的，下一行把全组名单写上。

**修改建议**：
- 第一行：答辩人/主讲人：XXX
- 第二行：小组成员：XXX、XXX、XXX
- 如果有指导老师，可加第三行：指导老师：XXX

---

### Slide 4：背景介绍（问题引入页）
**批注**：背景介绍部分需要有明确的逻辑，建议按下面三页组织：
1. 第一页介绍背景，引入“边缘计算”概念，说明其定义目标是为了降低时延；
2. 第二页引入问题，说明现有边缘计算已解决单个设备的比例决策问题，但现实是多设备多服务器竞争，已有范式不够用；
3. 第三页总结研究目标和技术方案（MARL分布式竞争）。

**修改建议**：
- **Slide 4 改为“背景：边缘计算”**：保留边缘计算定义、MEC 架构、任务卸载流程，突出“降低时延”这个核心目标。
- **新增一页“问题：从单设备到多设备竞争”**：
  - 现有工作：单设备场景下已解决“本地算多少、上传多少”的比例决策；
  - 现实挑战：多个 MD 共享多个 ES，存在资源竞争和协作需求；
  - 集中式调度 vs 分布式方案：我们选择分布式+MARL，追求灵活性和可扩展性。
- **Slide 6“项目目标”改为“目标：用 MARL 学会竞争与协作”**：
  - 目标：每个设备学会在“选最快服务器”和“避免拥挤”之间平衡；
  - 方案：分布式 MARL，每个 MD 作为独立智能体决策。

---

### Slide 15：传统 MLP 局限性
**批注**：注意看下面三页的标题，Pre 的时候建议按照这个逻辑串起来讲。

**修改建议**：确保三页逻辑连贯：
1. Slide 15：现有方案（MLP）的可扩展性差、可观察性差；
2. Slide 16：引入 GNN + HyperNetwork 两种可扩展架构；
3. Slide 17：具体设计方案（网络结构、正交化接口）。
- 每页开头加一句承上启下的话，例如 Slide 16 开头加“为解决上述问题，我们引入两种可扩展网络架构”。

---

### Slide 19 / 20：实验结果页面
**批注**：
1. 建议统一改为实验 1 的格式，明确指出：实验名称、指标类型、数据特点、基本结论，这四个必要要素；
2. 实验 1 是指训练过程的最优 Cost 吗？实验 2 是指训练过程的平均 Cost，这个有什么意义吗？实验 3 我没理解是干啥的。

**修改建议**：
- **统一三个实验的呈现格式**：
  - **实验名称**：要验证什么（如“同配置下不同网络结构的收敛性能对比”）；
  - **指标类型**：最优训练 Cost / 最终评估 Cost（100 episodes 均值）/ 完成率；
  - **数据特点**：训练配置、测试配置、训练轮数；
  - **基本结论**：一句话总结。

- **重新定义三个实验**：
  - **实验 1：同配置收敛对比**
    - 名称：三种网络（Standard/GNN/Hyper）在 2ES-3MD / 2ES-5MD / 3ES-7MD 下的收敛性能；
    - 指标：训练过程中 100 episodes 滑动平均 Cost 的最小值；
    - 数据：10K episodes，3 种配置 × 3 种网络；
    - 结论：GNN/Hyper 在复杂配置下优于 Standard MLP，Hyper 训练峰值最低但后期可能退化。
  - **实验 2：最终评估对比**
    - 名称：训练完成后在各自训练配置上的最终评估；
    - 指标：100 个未见 episodes 的平均 Cost ± std 和完成率；
    - 数据：取最终模型（非最优 checkpoint）评估；
    - 结论：最终模型不一定等于最优 checkpoint，反映过拟合/训练稳定性问题。
  - **实验 3：跨配置泛化对比**
    - 名称：在 3ES-7MD 上训练的 GNN 模型，迁移到 2ES-3MD / 2ES-5MD 的泛化能力；
    - 指标：跨配置 Cost 与完成率；
    - 数据：IPPO+GNN / ExplabOff+GNN；
    - 结论：ES-aware GNN 能根据 ES 算力选择服务器，泛化能力优于 index-based GNN。

- **建议删除或合并“单配置最优结果”这类模糊表述**，统一用“训练最优”和“最终评估”区分。

---

### Slide 21：关键发现
**问题**：当前第 1 条“ExplabOff+GNN 泛化最优”基于旧数据，最新 ES-aware GNN 实验显示 IPPO+GNN 在跨小配置上也不错。

**修改建议**：
- 更新为最新结论：
  1. **ES-aware GNN 学到了 ES 算力语义**：反转 ES 顺序实验显示模型选择最快 ES，而非最高动作索引；
  2. **训练最优 ≠ 泛化最优**：HyperNetwork 训练峰值最低但后期退化，GNN 最终模型更稳定；
  3. **ExplabOff 的 MI 奖励在复杂环境中提升完成率**：3ES-7MD 上 ExplabOff+GNN 完成率优于 IPPO+GNN。

---

### Slide 22：总结与反思
**批注**：
1. 思考 1 中，ExplabOff 是基线论文，IPPO 是作为基线吗，还是本项目的设计，有点不理解这个对比的意义；
2. 思考 2 还行，有时间可以分析一下原因。

**修改建议**：
- **思考 1 重写**：
  - IPPO = 本项目复现的 baseline 算法（Independent PPO，每个设备独立优化）；
  - ExplabOff = 论文提出的 advanced 算法（在 IPPO 基础上增加 MI 奖励促进探索与协作）；
  - 对比意义：验证在论文环境上，MI 奖励是否真的能带来性能/泛化提升。结论：在简单环境提升有限，但在复杂环境（3ES-7MD）和泛化上 MI 有帮助。
- **思考 2 深化原因分析**：
  - 原因 1：训练时只监控训练集 Cost，未监控验证集，导致过拟合到训练配置的 action index；
  - 原因 2：PPO 策略更新后期学习率过高，策略崩溃；
  - 原因 3：HyperNetwork/GNN 的 index-based action head 没有显式建模 ES 语义，跨配置时失效；
  - 改进：加入 ES-aware scoring、早停、学习率衰减、checkpoint 选择。

---

### Slide 23：结束页
**批注**：有点奇怪，建议改为“感谢观看”或者“恳请批评指正”，这样绝对没问题。

**修改建议**：
- 改为“恳请批评指正”更正式；
- 或保留“感谢观看”。

---

## 二、整体结构调整建议

### 当前结构问题
1. **背景部分逻辑跳跃**：从边缘计算直接跳到 MARL，缺少“为什么需要多设备分布式决策”的过渡；
2. **实验部分表述不一致**：三个实验的格式、指标、结论不统一，老师看不懂实验 2/3 的意义；
3. **思考页对比意义不清**：IPPO vs ExplabOff 的 baseline/advanced 关系没讲清楚；
4. **关键发现数据过时**：需要替换为最新 ES-aware GNN 实验结果。

### 建议调整后的结构

| 页码 | 内容 | 说明 |
| --- | --- | --- |
| 1 | 封面 | 标题、答辩人、成员、指导老师 |
| 2 | 目录 | 五个部分 |
| 3-4 | 背景：边缘计算与 MEC | 定义、目标（降低时延）、任务卸载流程 |
| 5-6 | 问题：从单设备到多设备竞争 | 现有工作局限、分布式竞争需求、MARL 方案 |
| 7-9 | 环境模型 | 系统架构、MARL 建模、观测/动作/奖励 |
| 10-13 | 算法详解 | PPO、IPPO、ExplabOff、正交化架构 |
| 14-17 | 研究创新 | MLP 局限、GNN/Hyper 方案、具体设计 |
| 18-21 | 实验结果 | 同配置收敛、最终评估、跨配置泛化、关键发现 |
| 22 | 总结与反思 | 两个思考，明确 baseline/advanced 关系 |
| 23 | 结束页 | 恳请批评指正 |

### 讲述逻辑建议
1. **“为什么”优先**：先讲清楚现实问题（多设备竞争），再引出 MARL；
2. **“baseline → advanced”明确**：IPPO 是 baseline，ExplabOff 是论文 advanced；
3. **实验“三段式”**：训练收敛 → 最终评估 → 跨配置泛化，层层递进；
4. **结论回扣问题**：最后强调 GNN/Hyper 解决了“跨配置可扩展”这个核心问题。

---

## 三、数据更新建议

建议用最新实验数据替换 Slide 19-21：

| 模型 | 3ES-7MD 训练最优 | 3ES-7MD 最终评估 | 5MD-2ES 泛化 | 3MD-2ES 泛化 |
| --- | --- | --- | --- | --- |
| IPPO + ES-aware GNN | 0.401 | 0.426 ± 0.012 | 0.446 | 0.497 |
| ExplabOff + ES-aware GNN | 0.407 | 0.428 ± 0.013 | 0.449 | 0.472 |
| IPPO + index-based GNN（旧） | 0.405 | 0.462 | 0.415 | 0.457 |
| ExplabOff + index-based GNN（旧） | 0.415 | 0.412 | 0.449 | 0.472 |

关键结论：ES-aware GNN 解决了 index heuristic 问题，泛化能力更可解释。

---

*以上为基于老师批注的修改意见，不直接修改 PPT，供你参考后手动调整。*
